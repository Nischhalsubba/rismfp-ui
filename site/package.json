{
  "name": "rismfp-ui",
  "version": "2.4.0",
  "private": true,
  "description": "Static historical interface for the Raising Incomes of Small and Medium Farmers Project.",
  "scripts": {
    "check:static": "node --check assets/js/flowchart.js && node --check assets/js/qa-fixes.js && node scripts/validate.mjs",
    "check:browser": "node scripts/browser-check.mjs",
    "check": "npm run check:static && npm run check:browser",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "1.61.1"
  },
  "license": "MIT"
}